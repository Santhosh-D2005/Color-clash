node_modules/
dist/
.build/
*.log
*.tsbuildinfo

# Test and diagnostic output.
test-results/
playwright-report/
eslint-report.json

# Build artefact — the signed outputs come from CI, not from the repo.
*.apk
*.aab
